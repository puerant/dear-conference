# 日程模块会场层级改造记录（待执行上线）

记录时间：2026-03-09  
状态：代码已完成，数据库迁移待执行（计划 2026-03-10 执行）

## 本次已完成
1. 界面中文化（管理端日程页）
- 已将日程管理页面英文文案统一替换为中文。
- 页面文件采用 UTF-8 编码（无 BOM），避免中文乱码。

2. 日程创建流程改造为“会场 -> 日程”
- 新增会场实体及管理接口：会场列表、创建、编辑、删除。
- 日程创建/更新由 `venue` 文本改为 `venueId` 强绑定。
- 后端增加校验：创建日程前必须存在有效会场。

3. 日程与日程项交互改为菜单式
- 左侧“日程列表”使用菜单展示。
- 右侧“日程项列表”使用菜单展示，并支持直接编辑/删除。
- 保留新增日程项弹窗流程。

## 关键变更文件
- 后端控制器：`backend/src/main/java/com/huiwutong/conference/controller/admin/AdminScheduleController.java`
- 后端服务接口：`backend/src/main/java/com/huiwutong/conference/service/ScheduleService.java`
- 后端服务实现：`backend/src/main/java/com/huiwutong/conference/service/impl/ScheduleServiceImpl.java`
- 日程实体：`backend/src/main/java/com/huiwutong/conference/entity/Schedule.java`
- 会场实体：`backend/src/main/java/com/huiwutong/conference/entity/ScheduleVenue.java`
- 会场 Mapper：`backend/src/main/java/com/huiwutong/conference/mapper/ScheduleVenueMapper.java`
- 日程 DTO：`backend/src/main/java/com/huiwutong/conference/service/dto/conference/ScheduleDto.java`
- 会场 DTO/VO：
  - `backend/src/main/java/com/huiwutong/conference/service/dto/conference/ScheduleVenueDto.java`
  - `backend/src/main/java/com/huiwutong/conference/service/dto/conference/ScheduleVenueVo.java`
- 前端 API：`frontend/admin/src/api/conference.ts`
- 前端页面：`frontend/admin/src/views/schedule/index.vue`
- 数据库迁移：
  - `docs/开发文档/sql/migration/11_schedule_venue_hierarchy.sql`

## 明天执行清单（2026-03-10）
1. 执行数据库迁移
- 执行脚本：`docs/开发文档/sql/migration/11_schedule_venue_hierarchy.sql`
- 目的：创建 `schedule_venue`、回填 `schedule.venue_id`、建立索引和外键约束。

2. 重启后端服务
- 让新增会场接口和 `venueId` 校验逻辑生效。

3. 联调回归
- 会场管理：新增/编辑/删除会场。
- 日程管理：未选会场不能建日程；已选会场可创建日程。
- 日程项管理：菜单展示、新增/编辑/删除正常。
- 兼容验证：历史数据可通过迁移回填到会场。

## 已完成技术验证
- 后端编译通过：`cd backend && mvn -q -DskipTests compile`
- 前端类型检查通过：`cd frontend/admin && npm run type-check`

