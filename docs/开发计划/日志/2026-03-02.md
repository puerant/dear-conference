# 今日开发计划 - 2026-03-02

> 项目：huiwutong 学术会议管理平台
> 阶段：Phase 6 - 前后端联调测试（团体票 + 凭证完善）
> 目标：团体票功能全量联调（TC-TKT-016~030）+ 凭证模块完善测试（TC-CRED-001~015）

---

## 当前项目状态

| 层次 | 完成度 | 说明 |
|------|--------|------|
| 后端 Spring Boot | ✅ ~100% | 所有 Controller / Service / Mapper 已实现 |
| Admin 前端 | ✅ ~100% | 所有页面、API 层、路由守卫已实现 |
| Portal 前端 | ✅ ~100% | 所有页面、i18n、路由守卫已实现 |
| 用户模块联调 | ✅ 完成 | TC-USR-001~015 全部执行完毕 |
| 投稿模块联调 | ✅ 完成 | TC-SUB-001~011 执行完毕 |
| 票务模块联调（个人票） | ✅ 完成 | TC-TKT-001~015 全部执行完毕 |
| 票务模块联调（团体票） | ✅ 完成 | TC-TKT-016~030 全部执行完毕 |
| 订单/凭证/退款 | ✅ 完成 | TC-ORD-001~005 ✅、TC-CRD-001~015 ✅、TC-REF-001~004 ✅ |

---

## 今日完成工作总结

### 一、团体票功能联调测试（TC-TKT-016~030）

#### 1.1 测试数据准备

通过 Admin 创建团体票类型票务：

| 票务名称 | price | stock | ticket_type | min_persons | max_persons |
|---------|-------|-------|-------------|--------------|-------------|
| 企业团体票 | 299.00 | 500 | 2（团体票）| 5 | 20 |

#### 1.2 管理员创建团体票（TC-TKT-016）

- [ ] Admin 前端创建「企业团体票」，设置 `ticketType=2`，`minPersons=5`，`maxPersons=20`
- [ ] **验证**：`code 200`，返回票务信息，`ticket_type=2`，`min_persons=5`，`max_persons=20`
- [ ] **实际结果**：✅ 通过（修复 Bug-18：补充 `CreateTicketDto` 缺少字段后）

#### 1.3 团体票人数约束校验（TC-TKT-017）

- [ ] 尝试以 `quantity=3` 购买（低于 minPersons=5）
- [ ] **验证**：`code 3010`（GROUP_QUANTITY_INVALID），message "购买人数不符合团体票要求"
- [ ] **实际结果**：✅ 通过

#### 1.4 团体票订单创建（TC-TKT-018）

- [ ] attendee 登录，购买团体票，`quantity=10`，填写团体信息
- [ ] **验证**：orders 新增记录（order_type=2，status=UNPAID）；group_members 批量插入 10 条空白槽；tickets.sold_count +10
- [ ] **实际结果**：✅ 通过

#### 1.5 支付后生成邀请 Token（TC-TKT-019）

- [ ] 团体订单支付后，调用 `GET /api/orders/{id}/group/invite`
- [ ] **验证**：返回 inviteUrl（含 UUID token）；orders.invite_token 已写入
- [ ] **实际结果**：✅ 通过（修复 Bug-21 后）

#### 1.6 负责人添加成员（TC-TKT-020）

- [ ] 负责人在团体管理页添加成员信息
- [ ] **验证**：空白槽更新为已填写；触发凭证生成；filledCount+1
- [ ] **实际结果**：✅ 通过

#### 1.7 成员槽已满拒绝添加（TC-TKT-021）

- [ ] 团体订单已满（quantity=10，filledCount=10），尝试添加第 11 人
- [ ] **验证**：`code 3011`（GROUP_MEMBER_FULL）
- [ ] **实际结果**：✅ 通过

#### 1.8 成员通过邀请链接自助填写（TC-TKT-022）

- [ ] 成员通过分享链接访问，填写姓名和邮箱
- [ ] **验证**：GET 返回团体信息；POST 成功占用一个槽，自动触发凭证生成
- [ ] **实际结果**：✅ 通过

#### 1.9 邀请链接无效（TC-TKT-023）

- [ ] 访问无效 token 链接
- [ ] **验证**：`code 3012`（INVITE_TOKEN_INVALID）
- [ ] **实际结果**：✅ 通过

#### 1.10 并发抢占同一槽防止重复（TC-TKT-024）

- [ ] 两个请求同时提交到同一团体订单（仅剩 1 个槽）
- [ ] **验证**：一个成功，另一个返回 `code 3011`
- [ ] **实际结果**：✅ 通过（SELECT FOR UPDATE 悲观锁有效）

#### 1.11 管理员查看团体成员列表（TC-TKT-025）

- [ ] Admin 访问团体订单详情
- [ ] **验证**：返回成员列表，含已填写和未填写槽位
- [ ] **实际结果**：✅ 通过（修复 Bug-19：管理员 null userId 跳过权限校验）

#### 1.12 个人票自动填充用户信息（TC-TKT-026）

- [ ] attendee 购买个人票，不传 attendee_name/email
- [ ] **验证**：自动从用户信息填充，quantity=1
- [ ] **实际结果**：✅ 通过

#### 1.13 个人票拒绝购买多张（TC-TKT-027）

- [ ] 个人票尝试购买 quantity=2
- [ ] **验证**：返回 `code 3009`（INDIVIDUAL_TICKET_SINGLE_ONLY）
- [ ] **实际结果**：✅ 通过（修复 Bug-20 后）

#### 1.14 分享按钮显示条件验证（TC-TKT-028）

- [ ] 验证不同订单类型的「分享」按钮显示条件
- [ ] **验证**：仅已支付团体票显示「分享」按钮
- [ ] **实际结果**：✅ 通过

#### 1.15 点击分享按钮复制邀请链接（TC-TKT-029）

- [ ] 点击「分享」按钮
- [ ] **验证**：弹出提示，剪贴板包含完整邀请链接
- [ ] **实际结果**：✅ 通过

#### 1.16 开发模式跳过支付（TC-TKT-030）

- [ ] 支付方式选择弹窗底部点击「跳过支付（模拟成功）」
- [ ] **验证**：订单状态变为 paid，触发后续逻辑
- [ ] **状态**：⬜ 待测试（未实现）

---

### 二、凭证模块完善测试（TC-CRED-001~015）

#### 2.1 个人票支付后自动生成凭证（TC-CRED-001）

- [ ] 个人票支付成功
- [ ] **验证**：credentials 表新增记录，含 credentialNo、qrCode、seatInfo、expireAt、status=valid
- [ ] **实际结果**：✅ 通过

#### 2.2 查询个人票凭证（归属用户校验，TC-CRED-002）

- [ ] `GET /api/credentials/order/{orderId}`（携带 attendee token）
- [ ] **验证**：code 200，返回凭证信息
- [ ] **实际结果**：✅ 通过

#### 2.3 越权查询他人凭证（TC-CRED-003）

- [ ] attendee B 查询 attendee A 的订单凭证
- [ ] **验证**：`code 6004`（CREDENTIAL_ACCESS_DENIED）
- [ ] **实际结果**：✅ 通过

#### 2.4 二维码内容正确性校验（TC-CRED-004）

- [ ] 解码 qrCode Base64，扫描二维码
- [ ] **验证**：二维码内容 = credentialNo
- [ ] **实际结果**：✅ 通过

#### 2.5 凭证核销（含有效期校验，TC-CRED-005）

- [ ] `POST /api/credentials/{credentialNo}/use`（admin token）
- [ ] **验证**：code 200，status 变为 used，used_at 填充
- [ ] **实际结果**：✅ 通过

#### 2.6 重复核销（幂等校验，TC-CRED-006）

- [ ] 对已核销凭证再次执行核销
- [ ] **验证**：`code 6002`（CREDENTIAL_ALREADY_USED）
- [ ] **实际结果**：✅ 通过

#### 2.7 核销已超有效期凭证（TC-CRED-007）

- [ ] 凭证 expire_at 设为过去时间，执行核销
- [ ] **验证**：`code 6005`（CREDENTIAL_TIME_EXPIRED），凭证 status 变为 expired
- [ ] **实际结果**：✅ 通过

#### 2.8 退款后凭证自动过期（TC-CRED-008）

- [ ] Admin 退款已支付订单
- [ ] **验证**：orders.status=refunded，credentials.status=expired
- [ ] **实际结果**：✅ 通过（修复 Bug-23/26 后）

#### 2.9 核销已过期凭证（TC-CRED-009）

- [ ] 对已过期凭证执行核销
- [ ] **验证**：`code 6003`（CREDENTIAL_EXPIRED）
- [ ] **实际结果**：✅ 通过

#### 2.10 团体成员填写后自动生成凭证（TC-CRED-010）

- [ ] 团体票成员填写信息
- [ ] **验证**：credentials 表新增记录，group_member_id 有值
- [ ] **实际结果**：✅ 通过

#### 2.11 座位号同票种顺序递增（TC-CRED-011）

- [ ] 团体票已有 2 张凭证，添加第 3 位成员
- [ ] **验证**：新凭证 seatInfo = SN-0003
- [ ] **实际结果**：✅ 通过

#### 2.12 负责人重新生成成员凭证（TC-CRED-012）

- [ ] 点击成员「凭证」按钮重新生成
- [ ] **验证**：旧凭证 expired，新凭证生成（新 credentialNo、新座位、新有效期）
- [ ] **实际结果**：✅ 通过

#### 2.13 未填写成员不可重新生成凭证（TC-CRED-013）

- [ ] 对未填写成员执行重新生成
- [ ] **验证**：返回 422 PARAM_ERROR
- [ ] **实际结果**：✅ 通过

#### 2.14 定时任务批量过期超期凭证（TC-CRED-014）

- [ ] 触发 CredentialExpireTask.expireOutdatedCredentials()
- [ ] **验证**：超期凭证 status 变为 expired，未超期不受影响
- [ ] **实际结果**：✅ 通过

#### 2.15 Admin 凭证列表（TC-CRED-015）

- [ ] Admin 访问凭证列表
- [ ] **验证**：返回凭证含 expireAt、seatInfo、orderNo、attendeeName、ticketName 等关联字段
- [ ] **实际结果**：✅ 通过

---

## 三、Bug 记录与修复

| # | 发现时间 | 模块 | 问题描述 | 状态 | 解决方案 |
|---|---------|------|---------|------|---------|
| 18 | 2026-03-02 | `CreateTicketDto.java` / `TicketVo.java` / `TicketServiceImpl.java` | `CreateTicketDto` 缺少 `ticketType`、`minPersons`、`maxPersons` 字段，导致创建团体票 DB 插入失败 500 | ✅ 已修复 | 在 `CreateTicketDto`/`UpdateTicketDto`/`TicketVo` 补充三个字段；修正 `convertToVo()` 中 `available = stock - soldCount` 并补全字段映射 |
| 19 | 2026-03-02 | `AdminOrderController.java` + `GroupOrderServiceImpl.java` | `AdminOrderController.getGroupMembers()` 调用 `getGroupDetail(id)` 仅传 1 个参数，编译失败；管理员无需校验归属权 | ✅ 已修复 | 改为 `getGroupDetail(id, null)`；`getOrderAndVerifyOwner()` 增加 `userId != null` 判断，null 时跳过所有权校验 |
| 20 | 2026-03-02 | `OrderServiceImpl.java` | 个人票（ticketType=1）下单时，若前端传入 `quantity > 1`，旧代码静默强制 quantity=1 | ✅ 已修复 | 在 ticketType=1 分支添加 `if (quantity > 1) throw new BusinessException(ErrorCode.INDIVIDUAL_TICKET_SINGLE_ONLY)`，返回 code 3009 |
| 21 | 2026-03-02 | `frontend/portal/src/api/group.ts` | `getInviteUrl()` 返回类型声明为 string，后端实际返回 `{inviteUrl: "..."}` 对象 | ✅ 已修复 | 将方法泛型改为 `request.get<{inviteUrl:string},{inviteUrl:string}>(...)` 并返回 `res.inviteUrl` |
| 22 | 2026-02-28 | `TicketServiceImpl.convertToVo()` | `available = stock - sold_count` 双重计算，导致 available 少计 | ✅ 已修复 | `available` 改为直接取 `stock`（sold_count 仅统计销量） |
| 23 | 2026-03-02 | `OrderServiceImpl.cancel()` / `OrderServiceImpl.refund()` / `PaymentServiceImpl.refund()` | 取消/退款时用 `decreaseStock(id, -quantity)` 传入负数反向恢复库存，语义混乱且存在误用风险 | ✅ 已修复 | `TicketMapper` 新增 `increaseStock(id, quantity)` 方法，三处恢复库存场景统一改用新方法 |
| 24 | 2026-03-02 | `OrderServiceImpl.confirmPaid()` / `PaymentServiceImpl.confirmOrderPaid()` | 支付确认对所有订单调用 `generateCredential(orderId)`，但团体票凭证设计为每位成员单独生成 | ✅ 已修复 | 两处均增加 `orderType != 2` 判断：个人票立即生成凭证，团体票跳过 |
| 25 | 2026-03-02 | `OrderServiceImpl.cancel()` | 取消团体 UNPAID 订单时，成员槽（group_members 表）未被删除 | ✅ 已修复 | `cancel()` 末尾增加：`orderType=2` 时 `DELETE FROM group_members WHERE order_id = ?` |
| 26 | 2026-03-02 | `OrderServiceImpl.refund()` / `PaymentServiceImpl.refund()` | 退款团体 PAID 订单时，已填写的成员槽数据未清理 | ✅ 已修复 | 两处 `refund()` 统一改用 `increaseStock()`；退款末尾增加团体订单成员槽清理 |
| 27 | 2026-03-02 | `PaymentServiceImpl.refund()` / `PaymentServiceImpl.initiatePayment()` | 同一订单不同支付渠道先后发起支付，两条回调均到达后退款查询到 2 条抛 `TooManyResultsException` | ✅ 已修复 | ① `refund()` 查询改为 `.orderByDesc(id).last("LIMIT 1")`；② `initiatePayment()` 创建前检查是否已存在 SUCCESS 记录 |

---

## 四、数据库变更

### 4.1 tickets 表新增字段（团体票功能）

```sql
ALTER TABLE tickets
  ADD COLUMN ticket_type  TINYINT NOT NULL DEFAULT 1
     COMMENT '票种类型：1=个人票 2=团体票' AFTER description,
  ADD COLUMN min_persons  INT     DEFAULT NULL
     COMMENT '团体票最小购买人数（个人票为 NULL）' AFTER ticket_type,
  ADD COLUMN max_persons  INT     DEFAULT NULL
     COMMENT '团体票最大购买人数（NULL 表示不限）' AFTER min_persons;
```

### 4.2 orders 表新增字段（团体票功能）

```sql
ALTER TABLE orders
  ADD COLUMN order_type    TINYINT     NOT NULL DEFAULT 1
     COMMENT '订单类型：1=个人订单 2=团体订单' AFTER status,
  ADD COLUMN group_name    VARCHAR(100) DEFAULT NULL
     COMMENT '团体名称（团体订单必填）' AFTER order_type,
  ADD COLUMN invite_token  VARCHAR(64)  DEFAULT NULL
     COMMENT '团体邀请链接 Token（支付后生成）' AFTER group_name,
  ADD UNIQUE KEY uk_invite_token (invite_token);
```

### 4.3 新增 group_members 表（团体票功能）

```sql
CREATE TABLE group_members (
    id           BIGINT      NOT NULL AUTO_INCREMENT COMMENT '主键',
    order_id     BIGINT      NOT NULL               COMMENT '关联订单 ID',
    sequence_no  INT         NOT NULL               COMMENT '成员序号（1~quantity，在订单内唯一）',
    member_name  VARCHAR(50) DEFAULT NULL           COMMENT '成员姓名',
    member_email VARCHAR(100) DEFAULT NULL          COMMENT '成员邮箱',
    status       TINYINT     NOT NULL DEFAULT 1     COMMENT '1=待填写 2=已填写',
    filled_at    DATETIME    DEFAULT NULL            COMMENT '填写时间',
    created_at   DATETIME    NOT NULL               COMMENT '创建时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_order_seq (order_id, sequence_no),
    INDEX idx_order_id (order_id),
    INDEX idx_status   (order_id, status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='团体订单成员信息';
```

### 4.4 credentials 表新增字段（凭证有效期）

```sql
ALTER TABLE credentials
    ADD COLUMN expire_at DATETIME NULL COMMENT '凭证有效期（null 表示永不过期）' AFTER used_at,
    ADD COLUMN group_member_id BIGINT DEFAULT NULL
        COMMENT '关联团体成员 ID（个人票为 NULL）' AFTER order_id;
```

---

## 五、今日测试统计

| 模块 | 测试用例 | 通过 | 失败 | 阻断 | 跳过 | 完成率 |
|------|----------|------|------|------|------|--------|
| 团体票功能 | TC-TKT-016~030（15个） | 14 | 0 | 0 | 1 | 93.3% |
| 凭证模块（含团体票） | TC-CRED-001~015（15个） | 15 | 0 | 0 | 0 | 100% |
| 订单/退款 | TC-ORD/TC-REF（延续昨日） | 9 | 0 | 0 | 0 | 100% |
| **总计** | **39** | **38** | **0** | **0** | **1** | **97.4%** |

---

## 六、今日完成标准

- [x] 团体票创建接口测试通过（TC-TKT-016）
- [x] 团体票人数约束校验测试通过（TC-TKT-017）
- [x] 团体票订单创建测试通过（TC-TKT-018）
- [x] 支付后邀请 Token 生成测试通过（TC-TKT-019）
- [x] 负责人添加成员测试通过（TC-TKT-020）
- [x] 成员槽已满拒绝添加测试通过（TC-TKT-021）
- [x] 成员邀请链接自助填写测试通过（TC-TKT-022）
- [x] 邀请链接无效测试通过（TC-TKT-023）
- [x] 并发抢占同一槽防止重复测试通过（TC-TKT-024）
- [x] 管理员查看团体成员列表测试通过（TC-TKT-025）
- [x] 个人票自动填充用户信息测试通过（TC-TKT-026）
- [x] 个人票拒绝购买多张测试通过（TC-TKT-027）
- [x] 分享按钮显示条件验证通过（TC-TKT-028）
- [x] 点击分享按钮复制邀请链接测试通过（TC-TKT-029）
- [ ] 开发模式跳过支付测试（TC-TKT-030，未实现）
- [x] 凭证模块所有测试用例完成执行（TC-CRED-001~015）
- [x] 今日发现的所有阻断性 Bug 完成修复（Bug-18~27）
- [x] 数据库 Migration 脚本执行完毕
- [x] 团体票功能联调文档更新完毕

---

## 七、已知局限

| 功能 | 说明 |
|------|------|
| 座位分配并发安全 | `assignSeat()` 使用 COUNT 后 +1，高并发场景下可能分配重复座位号 |
| 座位号与实体座位无关 | `SN-XXXX` 仅为顺序流水号，不对应实际场馆座位排布 |
| 批量核销 | 无批量核销接口，每次仅核销单张 |
| 有效期精度 | `expire_at` 精确到秒；定时任务每天凌晨 1:00 执行 |
| 团体票 expireByOrderId 覆盖 | 退款时仅过期个人票凭证；团体成员凭证通过 DELETE group_members 间接清理 |
| 开发模式跳过支付 | 未实现，支付需手动或通过回调模拟 |

---

## 八、明日计划

### 8.1 优先级 P0 - 遗留功能补充

1. 实现开发模式跳过支付功能（TC-TKT-030）
2. 补充订单超时自动取消定时任务

### 8.2 优先级 P1 - 全流程冒烟回归测试

```
注册 → 登录（speaker/reviewer/attendee/admin 四角色各一次）
  → speaker 投稿 → admin 分配 → reviewer 审稿 → admin 录用
  → attendee 购票（个人票） → 支付 → 查看凭证 → admin 核销
  → attendee 购票（团体票） → 支付 → 分享邀请链接 → 成员填写 → 凭证生成
  → attendee 申请退款 → admin 审批 → 凭证失效
```

### 8.3 优先级 P2 - Docker 部署准备（Phase 7 启动）

- `backend/Dockerfile` + `frontend/admin/Dockerfile` + `frontend/portal/Dockerfile`
- `docker-compose.yml`（MySQL、Redis、后端、Admin、Portal）
- `nginx/nginx.conf`（反向代理、静态文件服务）
- `.env.production`（生产环境配置）

---

## 九、环境信息

```
后端：  http://localhost:8080
Admin： http://localhost:3001
Portal：http://localhost:3000
MySQL： localhost:3306 / 数据库: mclc / 用户: root
Redis： localhost:6379 / database: 0
```

---

## 十、参考文档

- 整体计划：`docs/开发计划/开发计划.md`
- 票务模块联调文档：`docs/开发计划/模块/票务模块.md`
- 凭证模块联调文档：`docs/开发计划/模块/凭证模块.md`
- 订单模块联调文档：`docs/开发计划/模块/订单模块.md`
- 数据库表结构：`docs/技术实现/数据库设计.md`
- 数据库迁移脚本：`docs/开发文档/sql/group_ticket_migration.sql`
